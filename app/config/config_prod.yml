imports:
    - { resource: config.yml }

#framework:
#    validation:
#        cache: apc

# Doctrine Configuration
doctrine:
    dbal:
        path:     "%database_path%-prod"

monolog:
    handlers:
        main:
            type:         fingers_crossed
            action_level: error
            handler:      nested
        nested:
            type:  stream
            path:  "%kernel.logs_dir%/%kernel.environment%.log"
            level: debug
        console:
            type:  console
