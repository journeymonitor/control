{% extends '::base.html.twig' %}

{% block body %}

<div class="jumbotron">
<h1>Get a mail when your website breaks</h1>
<p class="lead">
    Upload your <span class="highlight">Selenium test script</span> and choose a check interval.<br>
    <strong>If the test fails, we email you.</strong></p>
</div>

<div class="row">
    <div class="col-lg-12">
        {% for flashMessage in app.session.flashbag.get('success') %}
            <div class="alert alert-success" role="alert">
                {{ flashMessage }}
            </div>
        {% endfor %}

        {% for flashMessage in app.session.flashbag.get('error') %}
            <div class="alert alert-danger" role="alert">
                {{ flashMessage }}
            </div>
        {% endfor %}

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>

<div class="row marketing">
    <div class="col-lg-6">
        <h4>What service do we provide?</h4>
        <p>Donec id elit non mi porta gravida at eget metus. Maecenas faucibus mollis interdum.</p>
    </div>

    <div class="col-lg-6">
        <h4>Subheading</h4>
        <p>Donec id elit non mi porta gravida at eget metus. Maecenas faucibus mollis interdum.</p>
    </div>
</div>

{% endblock %}
